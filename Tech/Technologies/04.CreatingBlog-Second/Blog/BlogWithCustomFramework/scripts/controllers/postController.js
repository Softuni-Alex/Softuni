class PostController{
    constructor(postView, requester, baseServiceUrl, appKey){
        this._postView = postView
        this._requester = requester
        this._appKey = appKey
        this._baseServiceUrl = baseServiceUrl
    }
}